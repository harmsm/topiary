name: topiary
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python<3.14
  - pip
  - numpy
  - pandas
  - xlrd
  - openpyxl
  - matplotlib
  - biopython
  - tqdm
  - dendropy
  - toytree
  - blast>=2.2
  - muscle>=5.0
  # openmpi is only needed because the bundled generax binary is compiled
  # against it; topiary itself no longer uses MPI. (mpi4py has been removed.)
  - openmpi<5
  - sphinx
  - sphinx_rtd_theme
  - pydata-sphinx-theme
  - pip:
    - opentree
    - ete4
